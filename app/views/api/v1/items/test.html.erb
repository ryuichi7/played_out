
"hi this is the test template"

<%= form_for :item, url: '/api/v1/items' do |f| %>
  <%= f.text_field :make %>
  <%= f.text_field :model %>
  <%= f.number_field :year %>

  <%= f.collection_select(:type_id, @types, :id, :name, prompt: true) %>

  <%= f.submit 'Submit' %>

<% end %>
